# Base URL of the FastAPI backend. The Vite dev server proxies /api to this
# target by default (see vite.config.ts), so you usually don't need to set
# this for local development — only for production builds.
VITE_API_BASE_URL=/api
